To use the widget add
import PaymentWidget
<PaymentWidget order={prop} />;

const prop = {
localAmount: Number,
originSender: String",
toChainId: Number,
toData:
String,
toTo: String,
toTransacting: String,
};
